library(devtools)
library(blogdown)

#blogdown::new_site(theme = 'user/repo') # old way to install

# blogdown::install_hugo() #new way to install (then restart rstudio)
# https://sourcethemes.com/academic/docs/install/#install-with-rstudio

